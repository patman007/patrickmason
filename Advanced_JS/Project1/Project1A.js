const teacher = ['a', 'b', 'c', 'd'];

const student = ['a', 'b', 'c', 'c'];

// ATTEMPT 6 - WITH Array.map() method


// ATTEMPT 7 - WITH FOR .. IN LOOP
var score = 0;
var x;
for (x in teacher) {
    console.log(teacher);
    if(teacher === student)
    score += score[x] + 4; 
}